import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function RequirementsBoardPage() {
  return (
    <PlaceholderPage
      title="需求看板"
      description="按状态分列的看板占位。"
    />
  );
}
