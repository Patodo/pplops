import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function RequirementsListPage() {
  return (
    <PlaceholderPage
      title="需求列表"
      description="表格视图、筛选与排序占位。"
    />
  );
}
